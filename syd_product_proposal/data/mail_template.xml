<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="email_template_proposal_sale" model="mail.template">
            <field name="name">Proposal Sales Order: Send by email</field>
            <field name="model_id" ref="syd_product_proposal.model_proposal_sale_order"/>
            <field name="subject">${object.company_id.name} ${'Proposal Order'} (Ref ${object.name or 'n/a' })</field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px; font-size: 13px;">
                        % set doc_name = 'Proposal Order'
                        Hello,
                        <br/>
                        <br/>
                        Your
                            ${doc_name} <strong>${object.name}</strong>
                            proposal amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}</strong> is ready for review.
                        <br/>
                        <br/>
                        <br/>
                        <center>
                            <a href="${object.get_portal_url()}"
                                target="_blank"
                                style="background-color: #875A7B; padding: 8px 16px 8px 16px; text-decoration: none; color: #fff; border-radius: 5px; font-size:13px;">
                                Proposal Order
                            </a>
                        </center>
                        <br/>
                        <br/>
                        <br/>
                        Do not hesitate to contact us if you have any questions.
                        <br/>
                    </p>
                </div>
            </field>
            <field name="report_template" ref="action_report_proposal_saleorder"/>
            <field name="report_name">${(object.name or '').replace('/','_')}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="True"/>
        </record>
    </data>
</odoo>